export interface IPropsInputText {
    value: string,
    placeholder: string,
    style?: any,
    type?: string,
    wrapperClass?: string,
    disabled?: boolean,
    onChange: Function,
    onClick?: Function
}